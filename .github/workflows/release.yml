# 🏴‍☠️ Velvet Chains Armada Release
# Tags → Build static site → Launch the Rust reliquary into crates.io → Cut GitHub Release with artifacts
name: "✨ Armada Release: Velvet Chains"

on:
  push:
    tags:
      - "v*" # Release on version tags like v1.2.3
  workflow_dispatch:
    inputs:
      tag:
        description: "Optional release tag to unfurl (e.g. v1.2.3)"
        required: false

permissions:
  contents: write
  actions: read

concurrency:
  group: release-${{ github.ref_name || inputs.tag }}
  cancel-in-progress: false

jobs:
  build-site:
    name: 🎭 Forge Static Site Artifact
    runs-on: ubuntu-latest
    steps:
      - name: ⚓ Hoist the Colors — Checkout
        uses: actions/checkout@v5
      - name: 🔮 Scry for Next.js Portal (app/pages)
        id: site
        run: |
          if [ -d app ] || [ -d pages ]; then
            echo "present=true" >> "$GITHUB_OUTPUT"
          else
            echo "present=false" >> "$GITHUB_OUTPUT"
            echo "No Next.js portal (app/ or pages/) detected; skipping site build." >&2
          fi

      - name: 🏴‍☠️ Prepare the Ship — Setup Node.js
        if: ${{ steps.site.outputs.present == 'true' }}
        uses: actions/setup-node@v6
        with:
          node-version: "20"
          cache: npm

      - name: 💎 Gather the Treasure — Install Dependencies
        if: ${{ steps.site.outputs.present == 'true' }}
        run: npm ci

      - name: 🧪 Validate Lore Codex (conditional)
        if:
          ${{ steps.site.outputs.present == 'true' && hashFiles('scripts/validate_lore.py') != '' }}
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install pyyaml
          npm run validate:lore

      - name: 📚 Build Lore JSON (conditional)
        if:
          ${{ steps.site.outputs.present == 'true' && hashFiles('scripts/build_lore_json.py') != ''
          }}
        run: npm run build:lore

      - name: ⚔️ Forge the Spectacle — Build
        if: ${{ steps.site.outputs.present == 'true' }}
        run: npm run build

      - name: 📦 Package Site Artifact
        if: ${{ steps.site.outputs.present == 'true' }}
        run: |
          VERSION="${GITHUB_REF_NAME:-manual}"
          ARCHIVE="velvet-chains-site-${VERSION}.tgz"
          tar -czf "$ARCHIVE" out
          sha256sum "$ARCHIVE" > "$ARCHIVE.sha256"

      - name: 📜 Upload Artifact for Release
        if: ${{ steps.site.outputs.present == 'true' }}
        uses: actions/upload-artifact@v7
        with:
          name: site-package
          path: |
            *.tgz
            *.tgz.sha256

  rust-binaries:
    name: ⚔️ Forge Rust Binaries
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            archive: tar.gz
          - target: x86_64-apple-darwin
            os: macos-latest
            archive: tar.gz
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            archive: zip
    steps:
      - name: 📦 Hoist the Colors — Checkout
        uses: actions/checkout@v5
      - name: 🔎 Detect Rust Engine
        id: rust
        shell: bash
        run: |
          if [ -f voidlight_engine/Cargo.toml ]; then
            echo "present=true" >> "$GITHUB_OUTPUT"
          else
            echo "present=false" >> "$GITHUB_OUTPUT"
            echo "No Rust engine found; skipping binaries for ${{ matrix.target }}" >&2
          fi
      - name: 🛠️ Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
        if: ${{ steps.rust.outputs.present == 'true' }}
      - name: 🔥 Build release binary
        if: ${{ steps.rust.outputs.present == 'true' }}
        run:
          cargo build --release --manifest-path voidlight_engine/Cargo.toml --target ${{
          matrix.target }}
      - name: 📦 Package binary (unix)
        if: ${{ steps.rust.outputs.present == 'true' && matrix.archive == 'tar.gz' }}
        run: |
          NAME="git-voidlight-${{ matrix.target }}"
          cp "voidlight_engine/target/${{ matrix.target }}/release/git-voidlight" "${NAME}"
          tar -czf "${NAME}.tar.gz" "${NAME}"
          sha256sum "${NAME}.tar.gz" > "${NAME}.tar.gz.sha256"
      - name: 📦 Package binary (windows)
        if: ${{ steps.rust.outputs.present == 'true' && matrix.archive == 'zip' }}
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $name = "git-voidlight-${{ matrix.target }}.exe"
          Copy-Item "voidlight_engine/target/${{ matrix.target }}/release/git-voidlight.exe" $name -Force
          $zip = $name -replace '\.exe$',''
          Compress-Archive -Path $name -DestinationPath "$zip.zip" -Force
          (Get-FileHash "$zip.zip" -Algorithm SHA256).Hash | Out-File -FilePath "$zip.zip.sha256" -Encoding ascii
      - name: 🚀 Upload binary artifact
        uses: actions/upload-artifact@v7
        with:
          name: rust-${{ matrix.target }}
          path: |
            git-voidlight-${{ matrix.target }}.tar.gz
            git-voidlight-${{ matrix.target }}.tar.gz.sha256
            git-voidlight-${{ matrix.target }}.zip
            git-voidlight-${{ matrix.target }}.zip.sha256
        if: ${{ steps.rust.outputs.present == 'true' }}

  python-package:
    name: 🐍 Build Python Package
    runs-on: ubuntu-latest
    steps:
      - name: 📦 Hoist the Colors — Checkout
        uses: actions/checkout@v5
      - name: 🔎 Detect Python package metadata
        id: py
        run: |
          if [ -f pyproject.toml ]; then
            echo "present=true" >> "$GITHUB_OUTPUT"
          else
            echo "present=false" >> "$GITHUB_OUTPUT"
            echo "No pyproject.toml found; skipping Python package build." >&2
          fi
      - name: 🔥 Setup Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: ✨ Setup uv
        uses: astral-sh/setup-uv@v7
      - name: 🌱 Create uv virtual environment
        if: ${{ steps.py.outputs.present == 'true' }}
        run: uv venv
      - name: 🔒 Verify uv.lock is up to date
        if: ${{ steps.py.outputs.present == 'true' && hashFiles('uv.lock') != '' }}
        run: uv lock --check
      - name: 🧰 Install build tooling
        if: ${{ steps.py.outputs.present == 'true' }}
        run: uv pip install build twine
      - name: 🎭 Build dist
        if: ${{ steps.py.outputs.present == 'true' }}
        run: uv run python -m build
      - name: 🚀 Upload python dist artifact
        uses: actions/upload-artifact@v7
        with:
          name: python-package
          path: dist/*
        if: ${{ steps.py.outputs.present == 'true' }}

  pypi-publish:
    name: 🐍 Unleash the Choreographer Upon the Moonlit PyPI Salon
    needs: [python-package]
    if: ${{ startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    steps:
      - name: ⚓ Hoist the Colors — Plunder the Full Tag Ledger
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: 🔎 Scry the Python Reliquary
        id: py
        run: |
          if [ -f pyproject.toml ]; then
            echo "present=true" >> "$GITHUB_OUTPUT"
          else
            echo "present=false" >> "$GITHUB_OUTPUT"
            echo "No pyproject prophecy was found; the Moonlit PyPI Salon shall remain untempted tonight." >&2
          fi

      - name: 🔖 Invoke the Release Sigil
        id: tag
        if: ${{ steps.py.outputs.present == 'true' }}
        run: |
          set -euo pipefail

          if [ -n "${{ inputs.tag }}" ]; then
            TAG_NAME="${{ inputs.tag }}"
          elif [[ "${GITHUB_REF:-}" == refs/tags/* ]]; then
            TAG_NAME="${GITHUB_REF_NAME}"
          else
            mapfile -t TAGS < <(git tag --points-at HEAD 'v*')

            if [ "${#TAGS[@]}" -eq 1 ]; then
              TAG_NAME="${TAGS[0]}"
            elif [ "${#TAGS[@]}" -gt 1 ]; then
              echo "⚠️ The moonlit salon hears too many release sigils whispered at once: ${TAGS[*]}" >&2
              echo "Name the chosen sigil explicitly with workflow_dispatch input 'tag'." >&2
              exit 1
            else
              echo "⚠️ No v* release sigil answered the summoning for this Python rite." >&2
              echo "Dispatch with the 'tag' input or invoke this rite from a tagged commit." >&2
              exit 1
            fi
          fi

          echo "name=${TAG_NAME}" >> "$GITHUB_OUTPUT"

      - name: 📜 Read the Choreographer's Prophecy
        id: pkg
        if: ${{ steps.py.outputs.present == 'true' }}
        run: |
          python3 - <<'PY'
          import os
          import tomllib
          from pathlib import Path

          pyproject = Path("pyproject.toml")
          data = tomllib.loads(pyproject.read_text())
          project = data["project"]

          github_output = Path(os.environ["GITHUB_OUTPUT"])
          with github_output.open("a", encoding="utf-8") as fh:
              fh.write(f'name={project["name"]}\n')
              fh.write(f'version={project["version"]}\n')
          PY

      - name: 🧭 Judge the Choreographer's Worthiness Before PyPI
        id: publish
        if: ${{ steps.py.outputs.present == 'true' }}
        run: |
          set -euo pipefail
          EXPECTED_TAG="v${{ steps.pkg.outputs.version }}"
          ACTUAL_TAG="${{ steps.tag.outputs.name }}"

          if [ "$EXPECTED_TAG" = "$ACTUAL_TAG" ]; then
            echo "allowed=true" >> "$GITHUB_OUTPUT"
            echo "The choreographer's true name aligns with the release sigil: $ACTUAL_TAG"
          else
            echo "allowed=false" >> "$GITHUB_OUTPUT"
            echo "The Moonlit PyPI Salon declines this dance for now: package ${{ steps.pkg.outputs.name }} bears version ${{ steps.pkg.outputs.version }}, but the release sigil is $ACTUAL_TAG."
          fi

      - name: 📥 Summon the Dist Relics from the Vault
        if: ${{ steps.py.outputs.present == 'true' && steps.publish.outputs.allowed == 'true' }}
        uses: actions/download-artifact@v8
        with:
          pattern: python-package
          path: python-dist
          merge-multiple: true

      - name: 🔐 Demand the PyPI Blood-Sigil
        if: ${{ steps.py.outputs.present == 'true' && steps.publish.outputs.allowed == 'true' }}
        env:
          PYPI_API_TOKEN: ${{ secrets.PYPI_API_TOKEN }}
        run: |
          if [ -z "${PYPI_API_TOKEN:-}" ]; then
            echo "PYPI_API_TOKEN is required before ${{ steps.pkg.outputs.name }} ${{ steps.pkg.outputs.version }} may whirl across PyPI." >&2
            exit 1
          fi

      - name: 💋 Unleash the Choreographer Upon PyPI
        if: ${{ steps.py.outputs.present == 'true' && steps.publish.outputs.allowed == 'true' }}
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: python-dist
          password: ${{ secrets.PYPI_API_TOKEN }}

  cargo-publish:
    name: 🌋 Hurl the Voidlight Reliquary into the Star-Forged Bazaar
    needs: [rust-binaries]
    if: ${{ startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    steps:
      - name: ⚓ Hoist the Colors — Plunder the Full Tag Ledger
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: 🔎 Scry the Engine Reliquary
        id: rust
        run: |
          if [ -f voidlight_engine/Cargo.toml ]; then
            echo "present=true" >> "$GITHUB_OUTPUT"
          else
            echo "present=false" >> "$GITHUB_OUTPUT"
            echo "No Rust engine reliquary found; the star-bazaar shall receive no offering tonight." >&2
          fi

      - name: 🛠️ Temper the Iron Heart
        if: ${{ steps.rust.outputs.present == 'true' }}
        uses: dtolnay/rust-toolchain@stable

      - name: 🔖 Invoke the Release Sigil
        id: tag
        if: ${{ steps.rust.outputs.present == 'true' }}
        run: |
          set -euo pipefail

          if [ -n "${{ inputs.tag }}" ]; then
            TAG_NAME="${{ inputs.tag }}"
          elif [[ "${GITHUB_REF:-}" == refs/tags/* ]]; then
            TAG_NAME="${GITHUB_REF_NAME}"
          else
            mapfile -t TAGS < <(git tag --points-at HEAD 'v*')

            if [ "${#TAGS[@]}" -eq 1 ]; then
              TAG_NAME="${TAGS[0]}"
            elif [ "${#TAGS[@]}" -gt 1 ]; then
              echo "⚠️ The void howls with too many release sigils on this commit: ${TAGS[*]}" >&2
              echo "Name the chosen sigil explicitly with workflow_dispatch input 'tag'." >&2
              exit 1
            else
              echo "⚠️ No v* release sigil answered the summoning for this run." >&2
              echo "Dispatch with the 'tag' input or invoke this rite from a tagged commit." >&2
              exit 1
            fi
          fi

          echo "name=${TAG_NAME}" >> "$GITHUB_OUTPUT"

      - name: 📜 Read the Crate Prophecy
        id: crate
        if: ${{ steps.rust.outputs.present == 'true' }}
        run: |
          python3 - <<'PY'
          import tomllib
          from pathlib import Path

          cargo_toml = Path("voidlight_engine/Cargo.toml")
          data = tomllib.loads(cargo_toml.read_text())
          package = data["package"]

          github_output = Path(__import__("os").environ["GITHUB_OUTPUT"])
          with github_output.open("a", encoding="utf-8") as fh:
              fh.write(f'name={package["name"]}\n')
              fh.write(f'version={package["version"]}\n')
          PY

      - name: 🧭 Judge the Crate's Worthiness Before the Bazaar
        id: publish
        if: ${{ steps.rust.outputs.present == 'true' }}
        run: |
          set -euo pipefail
          EXPECTED_TAG="v${{ steps.crate.outputs.version }}"
          ACTUAL_TAG="${{ steps.tag.outputs.name }}"

          if [ "$EXPECTED_TAG" = "$ACTUAL_TAG" ]; then
            echo "allowed=true" >> "$GITHUB_OUTPUT"
            echo "The reliquary's true name aligns with the release sigil: $ACTUAL_TAG"
          else
            echo "allowed=false" >> "$GITHUB_OUTPUT"
            echo "The star-bazaar rejects this offering for now: crate ${{ steps.crate.outputs.name }} bears version ${{ steps.crate.outputs.version }}, but the release sigil is $ACTUAL_TAG."
          fi

      - name: 🔐 Demand the crates.io Blood-Sigil
        if: ${{ steps.rust.outputs.present == 'true' && steps.publish.outputs.allowed == 'true' }}
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          if [ -z "${CARGO_REGISTRY_TOKEN:-}" ]; then
            echo "CARGO_REGISTRY_TOKEN is required before ${{ steps.crate.outputs.name }} ${{ steps.crate.outputs.version }} may be hurled into crates.io." >&2
            exit 1
          fi

      - name: 🚀 Hurl the Voidlight Reliquary into crates.io
        if: ${{ steps.rust.outputs.present == 'true' && steps.publish.outputs.allowed == 'true' }}
        working-directory: voidlight_engine
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: cargo publish --locked --token "$CARGO_REGISTRY_TOKEN"

  github-release:
    name: 🌌 Unfurl Cosmic Release Scroll
    needs: [build-site, rust-binaries, python-package]
    # Tag pushes release immediately; manual rites may provide a tag or run from a tagged commit.
    if: ${{ startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    steps:
      - name: ⚓ Hoist the Colors — Checkout Full Tag Ledger
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: 🔖 Resolve Tag Name
        id: tag
        run: |
          set -euo pipefail

          if [ -n "${{ inputs.tag }}" ]; then
            TAG_NAME="${{ inputs.tag }}"
          elif [[ "${GITHUB_REF:-}" == refs/tags/* ]]; then
            TAG_NAME="${GITHUB_REF_NAME}"
          else
            mapfile -t TAGS < <(git tag --points-at HEAD 'v*')

            if [ "${#TAGS[@]}" -eq 1 ]; then
              TAG_NAME="${TAGS[0]}"
            elif [ "${#TAGS[@]}" -gt 1 ]; then
              echo "⚠️ The cosmic brig found multiple release sigils on this commit: ${TAGS[*]}" >&2
              echo "Choose one explicitly with workflow_dispatch input 'tag'." >&2
              exit 1
            else
              echo "⚠️ No v* release tag was found for this run." >&2
              echo "Dispatch with the 'tag' input or invoke this rite from a tagged commit." >&2
              exit 1
            fi
          fi

          echo "name=${TAG_NAME}" >> "$GITHUB_OUTPUT"
      - name: 📥 Summon all Relics from the Vault
        uses: actions/download-artifact@v8
        with:
          pattern: "*"
          path: releases
          merge-multiple: true

      - name: 🔮 Scry Missing Checksums (Python dists)
        run: |
          shopt -s nullglob
          for f in releases/*.whl releases/*tar.gz; do
            [ -e "$f" ] || continue
            if [ ! -f "$f.sha256" ]; then
              sha256sum "$f" > "$f.sha256"
            fi
          done

      - name: 🧾 Inscribe the Assets Ledger
        run: |
          {
            echo "## Assets"
            echo
             echo "Filename | Size | SHA-256"
             echo "--- | ---: | ---"
             shopt -s nullglob
             for f in releases/*; do
               case "$f" in
                 *.sha256) continue;;
               esac
              SIZE_BYTES=$(stat -c%s "$f")
              SIZE_MB=$(awk -v s="$SIZE_BYTES" 'BEGIN{printf "%.2f MB", s/1024/1024}')
              if [ -f "$f.sha256" ]; then
                SUM=$(cut -d' ' -f1 < "$f.sha256")
              else
                SUM="(no checksum)"
              fi
              echo "$(basename "$f") | $SIZE_MB | \`$SUM\`"
            done | sort
            echo
          } > releases/RELEASE_BODY.md

      - name: 🌌 Publish Release to the Void
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.name }}
          target_commitish: ${{ github.sha }}
          files: |
            releases/*.tgz
            releases/*.tgz.sha256
            releases/git-voidlight-*.tar.gz
            releases/git-voidlight-*.tar.gz.sha256
            releases/git-voidlight-*.zip
            releases/git-voidlight-*.zip.sha256
            releases/voidlight_choreographer-*.whl
            releases/voidlight_choreographer-*.tar.gz
          body_path: releases/RELEASE_BODY.md
          generate_release_notes: true
          draft: false
          prerelease: ${{ contains(github.ref_name, '-') }}
